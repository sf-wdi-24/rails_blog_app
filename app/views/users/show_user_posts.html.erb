<h1><%= @user.name %> Posts</h1>
<% @posts.each do |post| %>
	<div class="thumbnail">
	<%= "Title: #{post.title}" %><br>
	<%= "Body: #{post.content}" %><br>
	<%= link_to "details", "/posts/#{post.id}" %><br>
	</div>
<% end %>