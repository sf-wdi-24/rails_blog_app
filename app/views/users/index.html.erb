<h1>Users:</h1>
<ul>

	<% @users.each do |user| %>
	<li>
		<%= user.email %>
		<%= button_to "Delete", user_path(user), method: :delete %>
		<%= link_to "Edit", edit_user_path(user)%>
	</li>
	<% end %>
</ul>

