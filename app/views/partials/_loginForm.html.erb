<%= form_tag '/login' do |f| %>
  <div class="row">
    <div class="large-12">
      Email: <%= text_field_tag :email %>
      Password: <%= password_field_tag :password %>
      <%= submit_tag "Login", class:"button expanded small" %>
    </div>
  </div>
<% end %>