<h1>Ayy</h1>

<h2>All Posts :</h2>
<% @posts.each do |post| %>
<p><%= post.title %> : <%= post.content %> posted by <%= post.user.name %></p>
<%= link_to "Update Post", edit_post_path(post) %>
<%= link_to "Delete Post", post_path(post), method: :delete %>
<% end %>